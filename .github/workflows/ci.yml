name: CI

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repository:
    name: Higiene do repositório
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código
        uses: actions/checkout@v7

      - name: Configurar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Executar testes de regressão
        run: node --test --test-isolation=none tests/repository-hygiene.test.mjs

  backend:
    name: Backend
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: sentinela
          POSTGRES_PASSWORD: sentinela
          POSTGRES_DB: sentinela
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U sentinela -d sentinela"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql://sentinela:sentinela@localhost:5432/sentinela

    steps:
      - name: Baixar código
        uses: actions/checkout@v7

      - name: Configurar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Bloquear vulnerabilidades críticas
        run: npm audit --audit-level=critical

      - name: Gerar cliente Prisma
        run: npm run prisma:generate

      - name: Validar migrations em PostgreSQL isolado
        run: npm run prisma:deploy

      - name: Executar testes
        run: npm test

      - name: Compilar
        run: npm run build

  mobile:
    name: Mobile
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile

    steps:
      - name: Baixar código
        uses: actions/checkout@v7

      - name: Configurar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Bloquear vulnerabilidades críticas
        run: npm audit --audit-level=critical

      - name: Validar dependências do Expo
        run: npx expo install --check

      - name: Executar testes
        run: npm test

      - name: Verificar tipos
        run: npm run typecheck

      - name: Executar lint
        run: npm run lint
